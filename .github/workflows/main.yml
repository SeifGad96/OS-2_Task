on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: 'docker login'
        run: echo "${{ secrets.DOCKER_TOKEN }}" | docker login --username seifgad96 --password-stdin