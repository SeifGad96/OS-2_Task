on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: 'docker login'
        run: docker login --username seifgad96 --password dckr_pat_rJw4MVmWOsBrY5TR7X3BsUDqT3E